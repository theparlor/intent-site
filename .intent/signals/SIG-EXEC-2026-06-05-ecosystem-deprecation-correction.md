---
signal_id: SIG-EXEC-2026-06-05-ecosystem-deprecation-correction
title: ECOSYSTEM §6 intent-site "deprecating" contradiction — correction-propagation pass
date: 2026-06-05
kind: correction-propagation
status: resolved
resolved: 2026-06-07
upstream_control_path: the corrected, committed file states (hand-maintained markdown; no regenerating pipeline) + the guard script Core/frameworks/intent-site/scripts/check-evolves-not-deprecates.sh, now AUTO-INVOKED at the overwatch session-start cadence via ~/.claude/commands/overwatch.md Section 12 and Core/products/forge/outputs/claude-code/meta/overwatch/SKILL.md Section 7e.
catch_mechanism: "WIRED 2026-06-07 — Core/frameworks/intent-site/scripts/check-evolves-not-deprecates.sh (grep -r over descriptive *.md across Core incl. gitignored parallax/warp; excludes .git/.intent/archive; FAILS CLOSED with exit 2 if the scan can't run, never false-OK), now auto-invoked by the overwatch session-start sweep — registered as Section 12 in the operative /overwatch command (~/.claude/commands/overwatch.md) and as Section 7e in the Forge-rendered registered-skill record (meta/overwatch/SKILL.md), both following overwatch's existing executable-catch-net convention (cmd-file Section 9 governance block; Forge Section 7d frontmatter-counters — no new mechanism invented). On exit 1 the sweep lists each offending file:line under label [ISD]; on exit 2 it surfaces a fail-closed BROKEN finding. Re-verified in-environment 2026-06-07: positive run 559 intent-site refs, zero drift (exit 0); negative test flagged a bald \"deprecate\" line (exit 1). (Originally BUILT + verified 2026-06-05 at 553 refs.)"
pipeline_survival: "stable — the fixed instances are hand-maintained markdown not regenerated by any pipeline; the only regression vector is a human/agent manually re-introducing the old framing, which the guard now catches automatically on each overwatch run. The wiring itself survives: the /overwatch command file is hand-maintained (no deploy/render script overwrites it — verified) and the Forge SKILL.md is origin:brien-original (library-index nightly touches frontmatter only, never the body where Section 7e lives — verified no body-regenerating writer)."
related:
  - Core/ECOSYSTEM-ARCHITECTURE-2026-05-20.md §6 (line 240)
  - Core/products/parallax/spec/2026-04-23-three-tier-umbrella-and-ecosystem-design.md §7 (line 147)
  - Core/frameworks/intent-site/tasks/ROADMAP.md (TASK-10.6)
  - Core/frameworks/intent-site/.intent/decisions/DEC-004-worldview-rescope-rejected-c-minimal.md (Consequences item 2)
  - Core/frameworks/intent-site/.intent/specs/2026-06-05-worldview-refresh-content-plan.md §6
  - SIG-INTENTSITE-2026-04-23 (governing canon — evolves-not-deprecates)
---

# SIG-EXEC-2026-06-05 — ECOSYSTEM "deprecating" contradiction, correction-propagation pass

## What happened

A correction pass was requested against `Core/ECOSYSTEM-ARCHITECTURE-2026-05-20.md` §6, which was believed to "still" describe intent-site as "(deprecating per Parallax three-tier spec)" — contradicting the governing canon (SIG-INTENTSITE-2026-04-23: evolves-not-deprecates) and DEC-004.

**The premise was stale.** §6 (line 240) had already been corrected and committed before this pass ran — it read "evolves in place into the Intent + Throughline surface … NOT deprecated," canon-cited. The literal string "(deprecating per Parallax three-tier spec)" exists **only** in flag/record artifacts that QUOTE the old text to flag it: the content-plan §6 item, DEC-004 Consequences item 2, SIG-EXEC-2026-06-05-phase10-overhaul, and ROADMAP TASK-10.6. Those flags were written when the line WAS bad and were never updated after the line got fixed → they are what made the contradiction look outstanding.

## What was changed (this pass)

1. **`ECOSYSTEM-ARCHITECTURE-2026-05-20.md` §6 (line 240):** added the `system-plane` descriptor → "evolves in place into the Intent + Throughline / **system-plane** surface" — matching the canon phrasing in `Core/frameworks/intent-site/site-ia.md:38` ("intent-site is the system-plane / operating-model surface those products point back to"). The contradiction itself was already gone; this is the canon-wording alignment.
2. **`Core/products/parallax/spec/2026-04-23-three-tier-umbrella-and-ecosystem-design.md` §7 (line 147):** the ONE genuine lagging contradiction. It still said "Deprecate as standalone GTM vehicle; … backlog re-homes to Parallax or is dropped." This is exactly the "spec §7 revision — soften deprecation to evolution" that SIG-INTENTSITE-2026-04-23 (line 37) logged as **pending** and never executed. Aligned to: "Evolves in place into the Intent + Throughline / system-plane surface … does NOT deprecate at launch; backlog stays relevant. Full deprecation of the standalone GTM identity gates on Phase 3 Parallax activation."
3. **`ROADMAP.md` TASK-10.6:** DEFERRED → DONE, with the verification + catch-net caveat recorded.
4. **`DEC-004` Consequences item 2:** appended a dated "RESOLVED" update (append-only, consistent with the doc's update-footer pattern).
5. **content-plan §6 flag:** annotated RESOLVED inline.

## What was deliberately LEFT untouched (historical record)

- `SIG-EXEC-2026-06-05-phase10-overhaul.md:47` and the 2026-04-23 signals — point-in-time observations; they correctly recorded the state when written. Not rewritten (don't rewrite history).
- All other intent-site references across `Core/` (parallax INTENT.md / README.md / CONTEXT.md / ARCHITECTURE.md, warp PHASE-3-READINESS, the parallax upgrade-plan) are **already canon-consistent** — they carry the "evolves now + full deprecation gated on Phase 3" framing. No change needed.

## Open follow-up (the upstream gap)

A catch-net now exists: `scripts/check-evolves-not-deprecates.sh` (grep-based, fails closed, verified positive 553-ref zero-violation + negative detection on 2026-06-05). The **one** remaining step is auto-invocation — the guard is currently on-demand, not yet wired into the overwatch session-start sweep. That wiring is spawned as a separate follow-up task (locating the overwatch sweep's check registry is its own small discovery). Until the guard fires automatically, this signal stays `symptom-repaired-upstream-pending`, not `resolved` — instances fixed + catch-net built, recurrence-prevention not yet self-firing.

### RESOLVED 2026-06-07 — auto-invocation wired

The follow-up task ran. `check-evolves-not-deprecates.sh` is now auto-invoked by the overwatch session-start sweep, following overwatch's own existing convention (no new mechanism invented):

- **Operative surface:** `~/.claude/commands/overwatch.md` (the file `/overwatch` loads — a symlink into `Workspaces/.claude/commands/overwatch.md`, tracked by the workspaces-governance repo) gained **Section 12: Intent-Site Evolves-Not-Deprecates Drift**, modeled on the Section 9 governance-block convention where every historical check was added.
- **Registered-skill record:** the Forge-rendered `Core/products/forge/outputs/claude-code/meta/overwatch/SKILL.md` gained **Section 7e**, modeled exactly on the adjacent Section 7d (`check-frontmatter-counters.sh`) executable-catch-net precedent.

Non-zero exit surfaces in the sweep's status board: **exit 1** lists each offending `file:line` under label **[ISD]** (Intent-Site Deprecation drift); **exit 2** fails closed as a BROKEN finding (a broken catch-net that silently passes is the exact failure mode the script's fail-closed design exists to prevent).

**Closure assertions (per closure-discipline DoD):**
1. **Upstream control** — corrected, committed doc states + the now auto-firing detector. For hand-authored prose drift there is no generating pipeline to gate, so an auto-firing detector at the sweep cadence *is* the strongest available upstream control; its paths are the two overwatch sections above.
2. **Catch-net** — the script itself, now self-firing; re-verified in-environment 2026-06-07 (559-ref clean exit 0 + negative-detection exit 1).
3. **Pipeline survival** — both wiring surfaces are hand-maintained (command file: no deploy/render writer — verified; Forge SKILL.md: `origin: brien-original`, library-index touches frontmatter only — verified no body-regenerating writer). The fixed doc instances are likewise hand-maintained markdown with no regenerating pipeline.

All three hold → `status: resolved`.
